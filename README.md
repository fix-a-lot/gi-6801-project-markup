# project-l-6801-markup

`project-l-6801`(ARKDEX) 원본 저장소에서 v0에게 필요한 마크업/디자인 관련 파일만 추출한 서브셋이다. DB 스키마, Open API 연동, 크론잡 등 로직 코드는 제외했다.

- 컴포넌트는 전부 `lib/mock-data.ts`의 mock 데이터로 동작한다.
- 원본과 동기화는 수동이다 — 원본에서 마크업/컴포넌트가 바뀌면 이 저장소에도 다시 복사해야 한다.
